// AutomatonSpec = { type: 'DFA'|'NFA', alphabet: string[], states: string[], start: string|null, finals: string[], transitions: {from,to,symbol}[] }
// DFA_ID = { state: string, unread: string }
// NFA_ID = { states: Set<string>, unread: string }
export {}